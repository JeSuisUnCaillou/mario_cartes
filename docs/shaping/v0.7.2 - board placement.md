# v0.7.2 Board placement

1. Depending on the screen size of the board client, the player might not be positioned correctly. Fix it.

2. I need a zone on top of the board screen to put several informations :
- A QR code for mobile to join as players -> use the current host name to build the link inside the qr code
- The name of the game "Mario Cartes"
- The id of the room

The QR code should have 15px paddings (white). the name of the game can be very big, and with big paddings. The bar has like 200px height.