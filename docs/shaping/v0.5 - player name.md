# V0.5 - player name

a player should have an input to enter his name on his screen (not a prompt, it's ugly). It should be 3 characteds in capitals like in super smash bros. store it in the player's game data in the backend.

On the board, the name of the player should be displayed on top of its helmet.

Display the player's name on top of its UI, and make it clickable to edit in place.
When the player updates its name, it should update it on the board.

The player name is always displayed as a seamless input.

We should store the player's name in the local storage.
When a player loads the player page, it should use his localstorage's name by default if it exists.