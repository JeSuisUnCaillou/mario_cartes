# V0.1 Client-server comms

I want to initialise a project with [Colyseus](https://github.com/colyseus/colyseus) for the backend.

Make a home page with a button to create a game
This should create a new [game room](https://docs.colyseus.io/room) in the colyseus server with a generated :id
Then the page goes to /game/:id/board, which displays the connected players live.

When another client opens /game/:id/player, he should join the room as a player.

Make a button in the player screen called "ping" which makes the board display a "ping" message for 1sec.


Make all this as simple as possible, it's just to setup the basics.
