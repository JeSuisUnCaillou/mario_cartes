# v0.10.1 - Feedbacks on turns

1. Display the ready state with a green checkmark emoji below the helmets, on top zone of the game board. Only during the lobby phase. The ready emoji and the cards of a player should take the same space, not be on top of each other. The cards actally replace the emoji when the game starts 

3. Add 15px padding inside the player's container, so that the border has more space

4. when i reload the board after the game has started, the ready icons reappar on top instead of the players cards. You should ALWAYS handle this kind of thing state-wise, so that we can ALWAYS reload the page at any moment and display the same state as before reloading.

5. the board-info-bar should be of fixed height, because otherwise the canvas is not the right size : after the players connect the board-info-bar changes height.

the board-info-bar should take a percentage of the screen's height. Make it 20%. Then the QR code should adapt to this size, and the board-top-element (and their content) too.

when a user clicks ready, on the board his helmet should not move up to make space for the ready icon. Make the ready icon present but hidden to fix that.

6. When a player is waiting, his "play zone" should be red-ish. And when he tries to play a card, the zone should animate its transparency back and forth to signal it's not his turn. 