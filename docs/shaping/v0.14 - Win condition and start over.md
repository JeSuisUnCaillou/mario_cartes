# v0.14 - Win condition and start over

Each player have a lap count, starting at 1.
When a player reaches the finish line once, the lap count increments.

There is 3 laps in a game.

The game has a ranking array, initialized empty.

When a player finishes his 3 laps, the game adds him to the ranking list.

When all players have finished their 3 laps, the game is over and a leaderboard is displayed, ranking the players (The first is the winner).

The leaderboard has a "start over" button that resets the game in its inital state.

# Feedbacks

"End turn" should be grey and disabled when a discard is awaited from a banana.

If a discard is awaited but the player has no cards in hand. In that case, the server should skip the discard for the player

lap count should actually start at zero, be incremented to 1 when first moving, be incremented to 2 when reaching the finish line once, be incremented to 3 when reaching the finish line a second time, and be incremented to 4 when reaching the finish line for the third time. In the display of the lap count, replace "lap : 4/3" by "1st", "2nd", "3rd" (etc) depending on the player's position on the ranking list 

reset the player ready state to false when restarting

in the player's client, the cards in the player's hand don't have the same position when he's active and when he's waiting. They should all have the positing they have when he's waiting.