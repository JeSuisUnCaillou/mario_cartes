# v1.3.1 - Redo hit animations after OOP refacto

## Hit animation (all simultaneous, effects follow avatar)
- Hit stars spawning out (inside avatar, follow if avatar moves)
- Helmet rotating TWICE (-720°)
- Dark mushroom spawning up if shell (inside avatar, follow if avatar moves)

## Player walks into banana/shell (sequential)
1. Player moves to cell
2. Item disappears + hit animation plays (simultaneous)
3. Cell reorganizes (item was still holding its space until now)

## Shell thrown at player (sequential)
1. Shell moves to player's cell
2. Shell disappears + hit animation plays (simultaneous)

## Shell thrown at banana/shell (already works)
1. Shell moves to target cell
2. Shell + target disappear + dust cloud (simultaneous)
3. Cell reorganizes

## Implementation
- Add `playHitEffect(shellHit)` to PlayerAvatar using a Phaser Container
- Container follows helmet position in scene `update()` loop
- Rewrite `animateItemHit`: moveDelay → destroy item + playHitEffect → after 1000ms tweenCellLayout
- Rewrite shell-thrown player hit: shell arrives → destroy shell + playHitEffect
- Move star/mushroom spawning into avatar, remove from BoardAnimator
